(function() {
  "use strict";

  /**
   * The bundle is used to provide linkage between modules
   * which may be combined into a single super-module.
   */
  var bundle = (typeof exports == "undefined") ? this : exports;

  bundle.Slickback = { VERSION: '0.1.0' };

}).call(this);
